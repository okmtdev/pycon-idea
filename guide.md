# 用語解説・補足ガイド

このドキュメントは `talk.md` に登場する用語やコンセプトを、前提知識がなくても理解できるよう詳しく解説するものです。

---

## 目次

1. [そもそもLLMとは何か](#1-そもそもllmとは何か)
2. [トークンとトークナイザ](#2-トークンとトークナイザ)
3. [BPE（Byte Pair Encoding）を図で理解する](#3-bpebyte-pair-encodingを図で理解する)
4. [tiktokenとは](#4-tiktokenとは)
5. [エンコーディング名の意味（cl100k_base, o200k_base）](#5-エンコーディング名の意味cl100k_base-o200k_base)
6. [コンテキストウィンドウとは](#6-コンテキストウィンドウとは)
7. [なぜトークン数が重要なのか](#7-なぜトークン数が重要なのか)
8. [Pythonコードにおけるトークン化の具体例](#8-pythonコードにおけるトークン化の具体例)
9. [snake_case と camelCase](#9-snake_case-と-camelcase)
10. [型ヒント（Type Hints）とは](#10-型ヒントtype-hintsとは)
11. [docstringとは](#11-docstringとは)
12. [リスト内包表記とは](#12-リスト内包表記とは)
13. [実験スクリプトの読み方](#13-実験スクリプトの読み方)
14. [このトークが伝えたいこと](#14-このトークが伝えたいこと)

---

## 1. そもそもLLMとは何か

**LLM（Large Language Model / 大規模言語モデル）** は、大量のテキストデータから「次にどんな単語が来るか」を予測するように訓練されたAIモデルです。

代表例:
- **GPT-4o** (OpenAI)
- **Claude** (Anthropic)
- **Gemini** (Google)

### LLMがコードを扱える理由

LLMの学習データには、GitHub上の大量のソースコードが含まれています。そのため、自然言語だけでなくプログラミング言語も「次にどんなトークンが来るか」を予測できます。

ただし重要なのは、**LLMはテキストをそのまま読んでいるわけではない**ということです。LLMに入力されるテキストは、まず**トークン**という単位に分割されてから処理されます。

---

## 2. トークンとトークナイザ

### トークンとは

**トークン（token）** とは、LLMがテキストを処理する際の最小単位です。

日常的な言葉でいうと「LLMにとっての1文字のようなもの」ですが、実際には文字よりも大きな単位です。

```
人間にとっての最小単位:   1文字（a, b, c, あ, い, う）
LLMにとっての最小単位:    1トークン（"hello", " world", "def", " calculate"）
```

### 具体例

英語の文 `"Hello, world!"` をトークン化すると:

```
入力テキスト:  "Hello, world!"
トークン列:    ["Hello", ",", " world", "!"]
トークン数:    4
```

ポイント:
- `"Hello"` は1トークン（よく使われる単語なのでまるごと1つ）
- `" world"` は**先頭にスペースを含めて**1トークン
- 句読点（`,` や `!`）はそれぞれ1トークン

### トークナイザとは

**トークナイザ（tokenizer）** は、テキストをトークン列に変換する（= トークン化する）プログラムです。

```
テキスト  ──→  [トークナイザ]  ──→  トークン列  ──→  [LLM本体]  ──→  応答
```

LLMモデルごとに専用のトークナイザがあり、同じテキストでもトークナイザが違えば異なるトークン列になります。

---

## 3. BPE（Byte Pair Encoding）を図で理解する

BPEは現在のLLMで最も広く使われるトークン化アルゴリズムです。

### 基本的な考え方

「よく一緒に出現する文字の並びをまとめて1つのトークンにする」という手法です。

### ステップバイステップの例

学習データが `"aaabdaaabac"` だけだとします。

```
ステップ0: 初期状態（1文字 = 1トークン）
  語彙: {a, b, c, d}
  テキスト: a a a b d a a a b a c

ステップ1: 最頻出ペアを探す → "a" + "a" が最多 (4回)
  新トークン "aa" を作成
  語彙: {a, b, c, d, aa}
  テキスト: aa a b d aa a b a c

ステップ2: 最頻出ペアを探す → "aa" + "a" が最多 (2回)
  新トークン "aaa" を作成
  語彙: {a, b, c, d, aa, aaa}
  テキスト: aaa b d aaa b a c

ステップ3: 最頻出ペアを探す → "aaa" + "b" が最多 (2回)
  新トークン "aaab" を作成
  語彙: {a, b, c, d, aa, aaa, aaab}
  テキスト: aaab d aaab a c
```

### 実際のLLMでのBPE

実際のLLMのトークナイザでは:
1. まずテキストを**UTF-8のバイト列**に変換する（全256種類のバイトが初期語彙）
2. 学習データ全体から最頻出バイトペアを見つけてマージ
3. 語彙サイズが目標（例: 100,000個）に達するまで繰り返す

結果として:
- **頻出する単語**（`the`, `def`, `return`）→ まるごと1トークン
- **やや珍しい単語**（`tokenizer`）→ 2〜3トークン（`token` + `izer`）
- **非常に珍しい単語や日本語**→ 多数のトークンに分割

---

## 4. tiktokenとは

**tiktoken** はOpenAIが公開しているPythonライブラリで、OpenAIのモデル（GPT-4, GPT-4oなど）が使うトークナイザを手元で実行できます。

### 何ができるのか

```python
import tiktoken

# GPT-4oが使うトークナイザを取得
enc = tiktoken.encoding_for_model("gpt-4o")

# テキストをトークン化
text = "def hello(): print('world')"
tokens = enc.encode(text)
print(tokens)        # [1404, 24748, 3932, 1374, 493, 1973, 340]
print(len(tokens))   # 7  ← この文は7トークン
```

### このトークでの使い方

本トークでは、tiktokenを使って「同じ意味のPythonコードでも、書き方によってトークン数がどれだけ変わるか」を可視化・実験します。

### なぜOpenAIのトークナイザを使うのか

- **公開されている**: tiktokenとしてだれでも使える
- **事実上の標準**: 他のLLMのトークナイザも似た仕組み（BPEベース）
- **定量的に比較可能**: 同じツールで統一的にトークン数を測定できる

※ Claudeなど他のモデルのトークナイザは非公開ですが、BPEベースである限り傾向は類似します。

---

## 5. エンコーディング名の意味（cl100k_base, o200k_base）

tiktokenでは「エンコーディング」という名前でトークナイザのバリエーションを区別します。

| エンコーディング名 | 語彙サイズ | 対応モデル | 特徴 |
|---|---|---|---|
| `cl100k_base` | ~100,000 | GPT-4, GPT-3.5-turbo | 広く使われた標準的なエンコーディング |
| `o200k_base` | ~200,000 | GPT-4o | 語彙が2倍。より多くのパターンを1トークンで表現可能 |

### 語彙サイズが大きいとどうなるか

語彙サイズが大きい = より多くの文字列パターンが「1トークン」として登録されている

```
例: "tokenizer" のトークン化

cl100k_base (語彙10万):  ["token", "izer"]     → 2トークン
o200k_base  (語彙20万):  ["tokenizer"]          → 1トークン（かもしれない）
```

語彙が大きいほどトークン効率は良くなりますが、モデル自体のサイズも大きくなるトレードオフがあります。

---

## 6. コンテキストウィンドウとは

**コンテキストウィンドウ（context window）** は、LLMが一度に処理できるトークン数の上限です。

### 身近なたとえ

コンテキストウィンドウは「LLMの作業机の広さ」のようなものです。

```
┌──────────────────────────────────────────────┐
│           コンテキストウィンドウ               │
│              (例: 128Kトークン)                │
│                                              │
│  ┌─────────┐ ┌──────────┐ ┌───────────────┐ │
│  │システム  │ │会話の     │ │あなたが渡した │ │
│  │プロンプト│ │やりとり   │ │コードファイル │ │
│  └─────────┘ └──────────┘ └───────────────┘ │
│                                              │
│  ┌──────────────────┐ ┌───────────────────┐  │
│  │LLMが考える       │ │LLMの応答          │  │
│  │スペース           │ │                   │  │
│  └──────────────────┘ └───────────────────┘  │
└──────────────────────────────────────────────┘
```

机が一杯になると、古い情報から順に「忘れて」いきます。そのため、同じ情報をより少ないトークンで表現できれば、より多くの情報を机の上に載せられるということになります。

### 主要モデルのコンテキストウィンドウ

| モデル | コンテキストウィンドウ |
|---|---|
| GPT-4o | 128,000 トークン |
| Claude 4 Opus | 200,000 トークン |
| Gemini 2.5 Pro | 1,000,000 トークン |

---

## 7. なぜトークン数が重要なのか

トークン数が重要な理由は大きく3つあります。

### 理由1: コンテキストウィンドウの制約

前述の通り、LLMに渡せる情報量には上限があります。コードのトークン数が多いほど、「渡せるコードの量」が減ります。

```
例: コンテキストウィンドウが128Kトークンのモデルに、
    ソースコードを渡して「バグを直して」とお願いする場合

効率的なコード:  500行 → ~3,000トークン → 余裕で渡せる
冗長なコード:   500行 → ~8,000トークン → 他の情報を削る必要あり
```

### 理由2: APIの課金

ChatGPT APIやClaude APIを使う場合、**トークン単位で課金**されます。同じ処理をさせるのに、トークン数が多いとコストが上がります。

### 理由3: 処理速度

LLMの応答速度はトークン数に比例します。入力トークンが多いほど、応答が返ってくるまでの時間が長くなります。

---

## 8. Pythonコードにおけるトークン化の具体例

### 例1: キーワードはだいたい1トークン

Pythonの予約語（`def`, `class`, `return`, `if`, `for` など）は、学習データに大量に出現するため、ほぼ確実に1トークンとして登録されています。

```
"def" → 1トークン
"return" → 1トークン
"import" → 1トークン
```

### 例2: インデントの扱い

Pythonはインデント（字下げ）が文法の一部です。

```
"    "（スペース4つ）→ 1トークン  ※頻出パターンなのでまとめられている
"        "（スペース8つ）→ 1〜2トークン
"\t"（タブ1つ）→ 1トークン
```

スペース4つのインデントが1トークンになるのは、Pythonの標準的なインデント幅が4スペースで、学習データに膨大な数が含まれているためです。

### 例3: 変数名の分割

```
"x"                → 1トークン（短い）
"result"           → 1トークン（一般的な英単語）
"calculate_total"  → "calculate" + "_total" = 2トークン
"my_var_name"      → "my" + "_var" + "_name" = 3トークン
```

アンダースコア `_` はトークンの境界になりやすいため、snake_caseの長い変数名はトークン数が増える傾向にあります。

### 例4: 日本語はトークンコストが高い

```
"hello"     → 1トークン
"こんにちは" → 3〜5トークン（トークナイザによる）
```

英語のLLMトークナイザは英語テキストに最適化されているため、日本語は1文字あたりのトークン消費量が英語よりはるかに多くなります。これが「日本語コメントはトークンコストが高い」と言われる理由です。

---

## 9. snake_case と camelCase

プログラミングにおける変数名・関数名の命名規則（naming convention）です。

### snake_case

単語をアンダースコア `_` でつなぐ方式。**Pythonの標準的な命名規則**です。

```python
def calculate_total_price(item_list):
    total_price = 0
    ...
```

### camelCase

先頭以外の単語の頭文字を大文字にする方式。**JavaScriptやJavaで一般的**です。

```python
def calculateTotalPrice(itemList):
    totalPrice = 0
    ...
```

### トークン化における違い

```
snake_case:  "calculate_total_price" → ["calculate", "_total", "_price"]  3トークン
camelCase:   "calculateTotalPrice"   → ["calculate", "Total", "Price"]    3トークン
                                  または ["calcul", "ateTotal", "Price"]   3トークン
```

実際のトークン数はケースバイケースですが、camelCaseの方がやや効率的になることがあります。ただし、Pythonではsnake_caseが慣例なので、LLMがPythonコードとして「自然」と感じるのはsnake_caseです。

---

## 10. 型ヒント（Type Hints）とは

Python 3.5以降で導入された、変数や関数の引数・戻り値に「型」の情報を記述する仕組みです。

### 型ヒントなし

```python
def add(a, b):
    return a + b
```

この関数は `a` と `b` に何が来るのか（数値？文字列？リスト？）がコードを読んだだけではわかりません。

### 型ヒントあり

```python
def add(a: int, b: int) -> int:
    return a + b
```

`: int` で「引数は整数」、`-> int` で「戻り値も整数」と明示しています。

### LLMにとっての型ヒントの意味

型ヒントがあると、LLMは関数の使い方をより正確に推測できます。

```python
# 型ヒントなし → LLMは文脈から推測するしかない
def process(data):
    ...

# 型ヒントあり → LLMは明確に理解できる
def process(data: list[dict[str, Any]]) -> DataFrame:
    ...
```

トークン数は増えますが、LLMの理解精度が向上するため、トレードオフとしては有利なことが多いです。

---

## 11. docstringとは

**docstring（ドキュメント文字列）** は、Pythonの関数・クラス・モジュールの先頭に書く説明文です。三重引用符 `"""..."""` で囲みます。

```python
def calculate_average(numbers: list[float]) -> float:
    """リスト内の数値の平均値を計算して返す。

    Args:
        numbers: 平均を求めたい数値のリスト。空リストの場合は0.0を返す。

    Returns:
        数値の平均値。
    """
    if not numbers:
        return 0.0
    return sum(numbers) / len(numbers)
```

### なぜdocstringがLLMフレンドリーなのか

1. **学習データに大量に存在する**: GitHubの良質なPythonプロジェクトには必ずdocstringがある
2. **構造化されたフォーマット**: LLMが解析しやすい決まった形式
3. **関数の直後に書く慣習**: コードと説明の対応関係が明確

通常のコメント（`# ...`）よりもdocstringの方が、LLMが「この関数は何をするのか」を理解する手がかりとして効果的です。

---

## 12. リスト内包表記とは

Pythonの特徴的な文法で、リストを簡潔に生成する記法です。

### forループで書く場合

```python
result = []
for x in range(10):
    if x % 2 == 0:
        result.append(x ** 2)
# result = [0, 4, 16, 36, 64]
```

### リスト内包表記で書く場合

```python
result = [x ** 2 for x in range(10) if x % 2 == 0]
# result = [0, 4, 16, 36, 64]
```

### トークン化の観点

リスト内包表記は1行で書けるため、forループ版に比べてトークン数が少なくなる傾向があります。また、Pythonの慣用的（イディオマティック）な書き方であり、LLMの学習データにも豊富に含まれているため、LLMにとっても理解しやすい表現です。

---

## 13. 実験スクリプトの読み方

### `visualize_tokens.py` の見方

このスクリプトを実行すると、コードが色分け表示されます。

```
Original code:
def add(a, b):
    result = a + b
    return result

Tokenized (each color = 1 token):
def| add|(|a|,| b|):|↵|    |result| =| a| +| b|↵|    |return| result|↵|

Token count: 18
```

- `|` で区切られた各部分が1トークン
- 色が違う部分はそれぞれ別のトークン
- `↵` は改行文字を表す

### `experiments.py` の見方

実行すると、各実験カテゴリの結果がテーブルで表示されます。

```
┌────────────────────────────────────────────────────────┐
│          variable_name_length (o200k_base)              │
├──────────────┬──────────┬───────┬───────┬───────┬──────┤
│ Label        │ Tokens   │ Chars │ Lines │Tok/Lin│Tok/Ch│
├──────────────┼──────────┼───────┼───────┼───────┼──────┤
│ 1char        │ 15(1.00x)│    32 │     3 │  5.00 │ 0.47 │
│ short        │ 16(1.07x)│    38 │     3 │  5.33 │ 0.42 │
│ moderate     │ 20(1.33x)│    52 │     3 │  6.67 │ 0.38 │
│ descriptive  │ 24(1.60x)│    83 │     3 │  8.00 │ 0.29 │
│ verbose      │ 32(2.13x)│   126 │     3 │ 10.67 │ 0.25 │
└──────────────┴──────────┴───────┴───────┴───────┴──────┘
```

各列の意味:
- **Label**: コードスニペットの名前
- **Tokens**: トークン数（`(1.60x)` は最少のものと比べた倍率）
- **Chars**: 文字数
- **Lines**: 行数
- **Tok/Line**: 1行あたりの平均トークン数
- **Tok/Char**: 1文字あたりの平均トークン数（「トークン効率」の指標。低いほど効率的）

---

## 14. このトークが伝えたいこと

### 核心のメッセージ

> **LLMは私たちのコードを「文字の並び」ではなく「トークンの並び」として見ている。**
> **この視点を持つだけで、LLMとの協働の質が変わる。**

### 実践的な示唆

| やること | 理由 |
|---|---|
| 変数名は適度な長さにする（1〜3単語） | 短すぎるとLLMが意味を推測できず、長すぎるとトークンを浪費する |
| docstringを英語で書く | LLMの学習データに豊富で、日本語よりトークン効率が良い |
| 型ヒントをつける | トークン増加分以上に、LLMの理解精度が向上する |
| Pythonの慣用的な書き方をする | LLMが最もよく見ているパターンなので正確に処理できる |
| 1ファイル・1関数を適度な大きさに保つ | コンテキストウィンドウを効率的に使える |
| 日本語コメントは重要な箇所に絞る | 英語の数倍のトークンを消費するため |

### このトークの新しさ

「良いコードを書こう」という話は昔からありますが、本トークでは**トークナイザという具体的なツールを使って定量的に測定する**というアプローチを取ります。「なんとなくこう書いた方が良い」ではなく、「このように書くとトークン数がN%減り、LLMの応答精度がこう変わる」という具体的なデータに基づいた議論ができるのがポイントです。

---

## 補足: 使われているPythonライブラリ

| ライブラリ | 用途 | 説明 |
|---|---|---|
| **tiktoken** | トークン化 | OpenAIが公開するトークナイザライブラリ。テキストをトークンに分割し、トークン数を数えられる |
| **rich** | ターミナル表示 | ターミナルにカラフルなテーブルや色付きテキストを表示するライブラリ。可視化デモで使用 |
| **matplotlib** | グラフ描画 | 棒グラフなどのチャートを生成するライブラリ。実験結果のグラフ出力に使用 |
