[project]
name = "pycon-llm-tokenization"
version = "0.1.0"
description = "LLMが見ているPythonの世界 ― トークナイザから逆算するコードの書き方"
requires-python = ">=3.11"
dependencies = [
    "tiktoken>=0.7.0",
    "rich>=13.0.0",
    "matplotlib>=3.8.0",
]

[tool.uv]
package = false
